apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  labels:
    app.kubernetes.io/name: order-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/instance: order-service
    app.kubernetes.io/version: "{{ .Values.services.order.image.tag }}"
    app.kubernetes.io/part-of: shop
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: order-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: order-service
        app.kubernetes.io/component: backend
        app.kubernetes.io/instance: order-service
        app.kubernetes.io/version: "{{ .Values.services.order.image.tag }}"
        app.kubernetes.io/part-of: shop
    spec:
      containers:
        - name: order-service
          image: "{{ .Values.services.order.image.repository }}:{{ .Values.services.order.image.tag }}"
          imagePullPolicy: {{ .Values.services.order.image.pullPolicy }}
          ports:
            - containerPort: 80
          resources:
            {{- toYaml $.Values.resources | nindent 12 }}
          env:
            - name: ServiceUrls__Inventory
              value: "http://inventory-service"
            - name: ServiceUrls__Payment
              value: "http://payment-service"
            - name: ServiceUrls__RabbitMQ__HostName
              value: "rabbitmq"
            - name: Serilog__WriteTo__0__Args__uri
              value: "http://observability-loki.observability.svc.cluster.local:3100"
            - name: Resilience__Enabled
              value: "false"
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: orderdb-psql-connection
                  key: connectionString
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 4
          livenessProbe:
            httpGet:
              path: /health/live
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 4